interface FormData {
    nameOf: string;
    amount: number;
    description: string;
    date: string;
    category: string;
    currency: string;
}

export default FormData; // Export the interface to make it accessible in other files
